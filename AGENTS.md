# AGENTS.md

## Agent skills

### Issue tracker

Local markdown under `.scratch/` — the canonical tracker. A GitHub repo exists at https://github.com/melkor42/Signal for code sharing; the tracker migrates there once `gh auth login` is completed. See `docs/agents/issue-tracker.md`.

Note: the project rebranded Signal → Atha; the GitHub repo rename is pending (external task), so the remote URL still reads `melkor42/Signal`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root, created lazily as terms and decisions get resolved. See `docs/agents/domain.md`.
